//Criar pasta de raiz

npm init
npm install express --save

//criar ficheiro
echo "" > server.js

//iniciar o servidor
node server.js

//num browser
escrever na barra de endere�o
http://localhost:3000/


//para o about
http://localhost:3000/about



//terminar o servidor

ctrl +c

pwd

ver caminho total da pasta


templates

install modulo hbs
https://handlebarsjs.com/

GIT

git init
criar .gitignore

Para guardar as versões:
git add .
git commit -m "mensagem"

git bash
gerar um par de chaves
ssh-keygen -t rsa -b 4096 -C '9160081@esmad.ipp.pt'

verificar
ls -la ~/.ssh/

adicionar o a chaves ao agente ssh
eval "$(ssh-agent -s)"
ssh-add

git push
